const form=document.querySelector('form');const error=document.querySelector('#authError');
function setStudent(student,token){localStorage.xbookStudent=JSON.stringify(student);localStorage.xbookStudentToken=token}
async function applyTheme(){try{const r=await fetch('/api/public');const d=await r.json();const s=d.settings||{},root=document.documentElement;const map={primary_color:'--mint',secondary_color:'--green2',accent_color:'--accent',light_bg:'--bg',dark_bg:'--dark',text_color:'--ink',dark_text_color:'--dark-ink',panel_color:'--panel',dark_panel_color:'--dark-panel'};Object.entries(map).forEach(([k,v])=>{if(s[k])root.style.setProperty(v,s[k])})}catch{}}
applyTheme();
if(form)form.addEventListener('submit',async e=>{e.preventDefault();error.textContent='';const body=Object.fromEntries(new FormData(form));const isRegister=form.id==='registerForm';try{const r=await fetch(isRegister?'/api/student/register':'/api/student/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw Error(d.error||'حدث خطأ');setStudent(d.student,d.token);const next=new URLSearchParams(location.search).get('next');location.href=next||'/account'}catch(x){error.textContent=x.message}});
